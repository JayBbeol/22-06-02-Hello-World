/*
#include <iostream>
#include <bitset>

using namespace std;

int main() {
	/*
	{
		cout << "     * * * *       " << endl;
		cout << "     *     *       " << endl;
		cout << "     *     *       " << endl;
		cout << "     *     *       " << endl;
		cout << "*                 *" << endl;
		cout << "*                 *" << endl;
		cout << "*                 *" << endl;
		cout << "*                 *" << endl;
		cout << "*                 *" << endl;
		cout << "* * * * * * * * * *" << endl;



		int A, B = {0};

		cout << "A is ?";
		cin >> A;
		cout << "B is ?";
		cin >> B;

		cout << "16진수 (A + B) = " << hex << uppercase << A + B << endl;
		*/


		/*//A-B가 0보다 크면 true, 0보다 작으면 false
		int A, B = { 0 };
		bool result = false;



		cout << "A is ?";
		cin >> A;
		cout << "B is ?";
		cin >> B;

		result = (A - B > 0) ? true : false;


		cout << "result = " << boolalpha << result << boolalpha << endl;*/


		/*
		int a, b, c = { 0 };

		cout << "a is ? ";
		cin >> a;
		cout << "b is ? ";
		cin >> b;
		cout << "c is ? ";
		cin >> c;

		if (a + b > c)
		{
			cout << a + b << endl;
		}
		else
		{
			cout << c << endl;
		}
	}
	*/
	/*
	int a = 0b10101010; // 2진법 표기법 ( 앞에 0b 붙이기 )
	int b = 0111; // 8진법 표기법 ( 앞에 0 붙이기 )
	int c = 0xff; // 16진법 표기법 ( 앞에 0x 붙이기)


	cout <<  a + b - c << endl;
	*/
	/*
	int a, b, c = { 0 };


	cout << "a is ? ";
	cin >> a;
	cout << "b is ? ";
	cin >> b;
	cout << "c is ? ";
	cin >> c;

	if (c > a && a > b)
	{
		cout << "true" << endl;
	}
	else
	{
		cout << "false" << endl;
	}

	*/


	
